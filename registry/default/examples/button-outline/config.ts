import type { RegistryItem } from '@/lib/schema'

const config: RegistryItem = {
  name: 'button-outline',
  title: 'Outline Button',
  type: 'registry:example',
  files: [
    {
      path: 'registry/default/examples/button-outline/button-outline.tsx',
      type: 'registry:example',
    },
  ],
}

export default config
