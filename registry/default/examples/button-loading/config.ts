import type { RegistryItem } from '@/lib/schema'

const config: RegistryItem = {
  name: 'button-loading',
  title: 'Button Loading',
  type: 'registry:example',
  files: [
    {
      path: 'registry/default/examples/button-loading/button-loading.tsx',
      type: 'registry:example',
    },
  ],
}

export default config
